pluginManagement {
    repositories {
        gradlePluginPortal()  // Ensure this is present
        google()
        mavenCentral()
        maven { url 'https://plugins.gradle.org/m2/' }  // Add this for Kotlin plugin
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}
include ':app'
